import React from "react";


//when loads, shows user's info in inputs
//user can edit information and either SAVE or CANCEL (buttons)

const EditUser = (props) => {


    return(
        <div>EditUser</div> 
    )
};

export default EditUser;